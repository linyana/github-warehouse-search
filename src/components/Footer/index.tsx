import { Layout } from "antd";
import React from "react";

const { Footer } = Layout;

const App: React.FC = () => (
  <Footer style={{ fontWeight: "700", color: "rgb(112, 116, 124)" }}>
    林衍
  </Footer>
);

export default App;
